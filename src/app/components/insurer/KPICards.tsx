// KPICards — live data only, no hardcoded numbers
// Used as a standalone export; InsurerDashboard uses KPIBar directly from live hook.
export { } // intentionally empty — all KPI rendering is in InsurerDashboard KPIBar
